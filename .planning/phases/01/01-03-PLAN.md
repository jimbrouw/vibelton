---
phase: 01-house-sub-genre-expansion
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - vibelton/harmony.py
  - vibelton/planner.py
autonomous: true
requirements:
  - SPEC §4
  - D-07
must_haves:
  truths:
    - "_EDM_STYLES in harmony.py contains 'deep_house' and 'tech_house'"
    - "genre_words() in planner.py contains 'deep house' and 'tech house' before 'house'"
    - "STYLE_PALETTES in planner.py contains 'deep_house' and 'tech_house' entries"
    - "expanded_song_sketch_plan() implements D-07 randomised intro strategy for deep_house and tech_house"
    - "D-07 seed is deterministic: same prompt always produces same intro style"
  artifacts:
    - path: "vibelton/harmony.py"
      provides: "2-chord drop behavior for deep_house and tech_house"
      contains: "deep_house"
    - path: "vibelton/planner.py"
      provides: "genre routing + STYLE_PALETTES + D-07 intro strategy"
      contains: "deep_house"
  key_links:
    - from: "expanded_song_sketch_plan()"
      to: "_EDM_STYLES"
      via: "style passed to plan_harmony() which checks _EDM_STYLES"
      pattern: "_EDM_STYLES"
    - from: "genre_words()"
      to: "expanded_song_sketch_plan()"
      via: "genre detection triggers expanded plan routing"
      pattern: "genre_words"
---

<objective>
Wire deep_house and tech_house into the Python backend: add both to harmony.py _EDM_STYLES, extend genre_words() and STYLE_PALETTES in planner.py, and implement D-07 randomised intro strategy in expanded_song_sketch_plan().

Purpose: After Plan 02's data model, this plan makes the backend generate-path use it correctly. Without this, deep_house prompts would not route to expanded_song_sketch_plan() and intro sections would all have the same character.
Output: harmony.py and planner.py updated; D-07 intro strategy active for deep_house and tech_house.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01/01-SPEC.md
@.planning/phases/01/01-CONTEXT.md
@.planning/phases/01/01-PATTERNS.md

<interfaces>
From .planning/phases/01/01-RESEARCH.md — verified line references:

harmony.py _EDM_STYLES (lines 295–300): frozenset or set literal containing "house", "uk garage", "techno", "trance", "drum n bass", "jungle", "dubstep", "grime", "trap". Add "deep_house" and "tech_house" to this set.

planner.py genre_words() (lines 1253–1279): list[str] of trigger phrases. "deep house" and "tech house" must be added BEFORE "house" (so sub-genre word matches fire before generic "house" catch). Also add before "techno".

planner.py STYLE_PALETTES (lines 1296–1404): dict[str, dict[str, list[str]]] mapping style keys to instrument name lists. Add "deep_house" and "tech_house" entries after "house" entry.

planner.py expanded_song_sketch_plan() (lines ~1150–1176): section loop that iterates (section, bars, energy) tuples. Existing energy-gating pattern: Bass skipped when energy=="intro", Riff only on build/main, Hook only on main/break. D-07 requires additional gating for deep_house and tech_house: intro sections randomly select from 3 intro styles keyed on sum(ord(c) for c in message) % 3.

planner.py _pattern_seed() pattern: deterministic seed derivation already used in the codebase — use sum(ord(c) for c in message) % 3 to match.

planner.py allowed_tracks branching (lines ~962–988): style-specific track set. Add "deep_house" and "tech_house" branches.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: harmony.py — add deep_house and tech_house to _EDM_STYLES</name>

  <read_first>
    vibelton/harmony.py — read the _EDM_STYLES definition (lines 295–305 per RESEARCH.md); confirm it is a set literal or frozenset, understand surrounding context
    .planning/phases/01/01-PATTERNS.md — harmony.py section (1 sub-task, the _EDM_STYLES set extension code)
  </read_first>

  <action>
    Add "deep_house" and "tech_house" to the _EDM_STYLES set literal. These two keys are the exact snake_case values returned by detect_style_from_text() — they must match what plan_harmony() receives as the style argument. Do not rename or add camelCase variants. The set already has a trailing comma style — follow the same pattern.
  </action>

  <verify>
    <automated>cd /Users/standard/Documents/New\ project\ 3 && python -c "from vibelton.harmony import _EDM_STYLES; print('deep_house' in _EDM_STYLES, 'tech_house' in _EDM_STYLES)"</automated>
    Expected: True True
  </verify>

  <done>
    - python -c "from vibelton.harmony import _EDM_STYLES; print('deep_house' in _EDM_STYLES, 'tech_house' in _EDM_STYLES)" prints True True
    - python -m pytest tests/ -x -q still passes (no regressions from the set change)
  </done>
</task>

<task type="auto">
  <name>Task 2: planner.py — genre_words, STYLE_PALETTES, and D-07 intro strategy</name>

  <read_first>
    vibelton/planner.py — read the full file in sections: (1) genre_words() function, (2) STYLE_PALETTES dict, (3) expanded_song_sketch_plan() function including the section loop and any allowed_tracks branching; understand the existing energy-gating logic before adding D-07
    .planning/phases/01/01-PATTERNS.md — planner.py Sub-tasks A, B, and C code excerpts (genre_words position, STYLE_PALETTES entry format, D-07 seed and gating logic)
    .planning/phases/01/01-CONTEXT.md — D-07 locked decision (three intro styles: drums_reveal, atmosphere, filtered)
  </read_first>

  <action>
    1. In genre_words(), add "deep house" before "house" in the list, and "tech house" before "techno" in the list. The exact position matters: sub-genre words must come before their parent catch-all so the any() check in genre detection fires on sub-genres first.

    2. In STYLE_PALETTES, add "deep_house" entry after "house" entry with these instrument lists — deep_house blends house instruments with jazz/Rhodes flavour: chords: ["Rhodes", "Electric", "Analog", "Meld"], pad: ["Warm House Pad", "Warm Pad", "Meld", "Analog"], riff: ["Soulful Riff", "Electric", "Drift", "Wavetable"], hook: ["House Lead", "Electric", "Drift", "Wavetable"], bass: ["Sub Bass", "Analog", "Drift", "Operator"], bd: ["909 Core Kit", "808 Core Kit"], hats: ["909 Core Kit", "808 Core Kit"]. Add "tech_house" entry after "deep_house" with techno-influenced lists: chords: ["Analog", "Drift", "Wavetable", "Operator"], pad: ["Dark Pad", "Tension", "Meld", "Wavetable"], riff: ["Hypnotic Riff", "Acid Riff", "Drift", "Operator"], hook: ["Minimal Hook", "Operator", "Drift", "Meld"], bass: ["Rolling Bass", "Driving Bass", "Operator", "Drift"], bd: ["909 Core Kit", "808 Core Kit"], hats: ["909 Core Kit", "808 Core Kit"].

    3. In expanded_song_sketch_plan(), implement D-07 randomised intro strategy. Before the section loop, compute the intro seed only when style is "deep_house" or "tech_house": intro_seed = sum(ord(c) for c in message) % 3 if style in ("deep_house", "tech_house") else None, with _INTRO_STRATEGIES = {0: "drums_reveal", 1: "atmosphere", 2: "filtered"} and intro_style = _INTRO_STRATEGIES[intro_seed] if intro_seed is not None else None. Inside the section loop, when energy == "intro" and intro_style is not None, gate clip generation as follows — "drums_reveal": only include Bd, Snare/Clap, Hh/Sh/Rd tracks (skip Chords, Pad, Bass, Riff, Hook, Ambience); "atmosphere": only include Chords, Pad, Ambience tracks (skip Bd, Bass, Riff, Hook, Hh/Sh/Rd); "filtered": all tracks included at existing energy="intro" velocity reduction (this is the existing behavior — no additional change needed for this case). The gating must not affect non-intro sections (energy != "intro") or styles other than deep_house/tech_house.

    4. If allowed_tracks branching exists in expanded_song_sketch_plan() for other styles, add "deep_house" and "tech_house" branches following the same pattern with the full track set: Chords, Pad, Bass, Riff, Hook, Bd, Snare/Clap, Hh/Sh/Rd, Percussion, Drum Instrument, Ambience.
  </action>

  <verify>
    <automated>cd /Users/standard/Documents/New\ project\ 3 && python -c "from vibelton.planner import genre_words, STYLE_PALETTES; gw = genre_words(); dh_idx = gw.index('deep house'); h_idx = gw.index('house'); th_idx = gw.index('tech house'); print('deep house before house:', dh_idx < h_idx, 'tech house before techno:', th_idx < gw.index('techno'), 'deep_house in palettes:', 'deep_house' in STYLE_PALETTES, 'tech_house in palettes:', 'tech_house' in STYLE_PALETTES)"</automated>
    Expected: deep house before house: True tech house before techno: True deep_house in palettes: True tech_house in palettes: True
  </verify>

  <done>
    - genre_words() list has "deep house" at lower index than "house", "tech house" at lower index than "techno"
    - STYLE_PALETTES["deep_house"] and STYLE_PALETTES["tech_house"] exist with at least bd, bass, chords keys each
    - expanded_song_sketch_plan() has intro_seed computation before the section loop when style in ("deep_house", "tech_house")
    - Same call with same prompt text produces same intro style (deterministic): python -c "from vibelton.planner import expanded_song_sketch_plan; r1 = expanded_song_sketch_plan('Create an expanded Deep House song sketch with soulful chords, warm bassline, late-night groove, and jazzy lead phrases.'); r2 = expanded_song_sketch_plan('Create an expanded Deep House song sketch with soulful chords, warm bassline, late-night groove, and jazzy lead phrases.'); print(r1 == r2)" prints True
    - python -m pytest tests/ -x -q passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| message string → intro_seed | User prompt text is hashed via ord() sum; not eval'd |
| style key → _EDM_STYLES lookup | Style strings are compared by value; no injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | intro_seed derivation | accept | sum(ord(c) for c in message) is safe hash; no code execution |
| T-01-06 | Denial of Service | expanded_song_sketch_plan infinite loop | accept | D-07 gating is conditional on energy=="intro" — only fires on intro sections; no loop added |
| T-01-07 | Information Disclosure | STYLE_PALETTES | accept | Instrument name lists are static constants; no user data exposed |
</threat_model>

<verification>
python -m pytest tests/ -x -q — full suite green
python -c verify command for genre_words ordering and STYLE_PALETTES keys — all True
D-07 determinism check passes
</verification>

<success_criteria>
- _EDM_STYLES contains "deep_house" and "tech_house"
- genre_words() has "deep house" before "house" and "tech house" before "techno"
- STYLE_PALETTES has "deep_house" and "tech_house" keys
- D-07 intro strategy is active in expanded_song_sketch_plan() for deep_house and tech_house only
- D-07 is deterministic per prompt text (same prompt → same intro style)
- python -m pytest tests/ -x -q exits 0
</success_criteria>

<output>
After completion, create .planning/phases/01-house-sub-genre-expansion/01-03-SUMMARY.md
</output>
