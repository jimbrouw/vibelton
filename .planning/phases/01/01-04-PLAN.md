---
phase: 01-house-sub-genre-expansion
plan: 04
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - lib/genreDNA.ts
  - app/layout.tsx
  - app/page.tsx
autonomous: true
requirements:
  - SPEC §5
  - D-01
  - D-02
  - D-03
  - D-04
must_haves:
  truths:
    - "lib/genreDNA.ts GENRES object has 'deepHouse' and 'techHouse' entries (camelCase)"
    - "app/layout.tsx imports web/styles.css so CSS tokens are available to app/page.tsx"
    - "app/page.tsx renders a Genre Cards section with House parent card (expandable, collapsed by default)"
    - "House parent card expands to show Deep House and Tech House sub-genre cards (D-01, D-02)"
    - "Clicking Deep House card sets userInput to the Deep House prompt template and genreId to 'deepHouse'"
    - "Clicking Tech House card sets userInput to the Tech House prompt template and genreId to 'techHouse'"
    - "Non-parent genre cards (Techno, UKG, Trap) are direct-click buttons with no expand behavior (D-03)"
    - "Expand animation is ~200ms ease (D-04)"
    - "Cards display label, subline, and description from SPEC §5.1 — exact copy, no rewrites"
  artifacts:
    - path: "lib/genreDNA.ts"
      provides: "TypeScript genre registry entries for deepHouse and techHouse"
      contains: "deepHouse"
    - path: "app/page.tsx"
      provides: "Genre card UI with expandable House parent and sub-genre buttons"
      contains: "genre-parent-card"
    - path: "app/layout.tsx"
      provides: "CSS token import for design system"
      contains: "web/styles.css"
  key_links:
    - from: "app/page.tsx genre card click"
      to: "POST /api/finisher"
      via: "genreId state + userInput state → form submit"
      pattern: "deepHouse|techHouse"
    - from: "app/layout.tsx"
      to: "web/styles.css"
      via: "import statement"
      pattern: "import.*web/styles\\.css"
---

<objective>
Implement the Next.js genre card UI and wire the TypeScript genre registry. Users with zero music vocabulary can select Deep House or Tech House via descriptive cards that fire pre-written prompts.

Purpose: The user-facing entry point for Phase 1. Without this, users must know genre vocabulary to use the sub-genres.
Output: lib/genreDNA.ts with deepHouse/techHouse entries, app/layout.tsx with CSS import, app/page.tsx with full genre card section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01/01-SPEC.md
@.planning/phases/01/01-UI-SPEC.md
@.planning/phases/01/01-CONTEXT.md
@.planning/phases/01/01-PATTERNS.md

<interfaces>
From .planning/phases/01/01-PATTERNS.md — verified patterns:

lib/genreDNA.ts GENRES object: Record<string, GenreDNA>. Existing keys use camelCase: "ukGarage", "house", "techno", etc. GenreDNA type fields: name, bpmRange, swingPercent, barGrid, structure, kickPattern, bassPattern, hiHatPattern, energyArc, rules, ruleBreakers, blendsWith, vibeWords. Add "deepHouse" and "techHouse" entries after existing "house" entry.

app/api/finisher/route.ts validation (lines 432–444): validates body.genreId against GENRES[body.genreId] — keys must be camelCase to pass. Genre card data-genreId or React state must use "deepHouse"/"techHouse".

app/layout.tsx current state (lines 1–9): imports only ReactNode type, renders bare html/body. Add import statement for CSS before the ReactNode import.

app/page.tsx current state (lines 1–8): 8-line stub with h1 and p tag. Replace entirely with genre cards section.

web/styles.css CSS tokens and classes used by new components:
- var(--bg), var(--ink), var(--muted), var(--panel), var(--panel-strong), var(--line), var(--accent)
- .prompt-button: display:grid; gap:18px; min-height:128px; border:1px solid var(--line); border-radius:8px; padding:15px; background:var(--panel-strong); color:var(--ink); text-align:left
- .prompt-button:hover: border-color with accent mix, box-shadow glow, transform:translateY(-2px)
- .vst-settings details expand pattern: summary h2::after content:"▸"; details[open] rotates 90deg; 200ms ease transition

Route to /api/finisher: POST with { genreId: string, userInput: string }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript genre registry — add deepHouse and techHouse to lib/genreDNA.ts</name>

  <read_first>
    lib/genreDNA.ts — read full file to understand the GenreDNA TypeScript type, GENRES object structure, existing entry format (ukGarage, house entries), and blendsWith value conventions (camelCase partner keys)
    .planning/phases/01/01-PATTERNS.md — lib/genreDNA.ts section with deepHouse and techHouse entry code excerpts
    .planning/phases/01/01-SPEC.md — §3.3 and §3.4 for exact field values; §5.1 for vibe words
  </read_first>

  <action>
    Add deepHouse entry after the existing "house" entry in the GENRES object. Fields (all values from SPEC §3.3 and D-05): name: 'Deep House', bpmRange: [120, 124], swingPercent: [60, 65], barGrid: '4/4 16-bar phrases', structure: ['intro 32', 'build 16', 'main 32', 'break 16', 'main 2 32', 'outro 32'], kickPattern: 'Patient four-on-the-floor with a soft ghost on beat 3. No double kicks.', bassPattern: 'Sub-bass focused. Octave drops on bar 3 give forward pull.', hiHatPattern: 'Open hat on upbeats or closed 8th-note drive.', energyArc: 'sustained', rules: 5 rules from SPEC, ruleBreakers: 2 rule breakers from SPEC, blendsWith: ['house', 'amapiano', 'ukGarage'], vibeWords: ['deep', 'soulful', 'late-night', 'rhodes', 'warm', 'hypnotic', 'jazzy'].

    Add techHouse entry after deepHouse. Fields (from SPEC §3.4 and D-06): name: 'Tech House', bpmRange: [127, 132], swingPercent: [50, 52], barGrid: '4/4 16-bar phrases', structure: ['intro 16', 'build 32', 'peak 32', 'reduction 16', 'peak 2 32', 'outro 16'], kickPattern: 'Relentless four-on-the-floor at full velocity. No variation, no mercy.', bassPattern: 'Rolling 16th-note riff. Tight and industrial. Bass is texture as much as groove.', hiHatPattern: '16th-note closed hats, high velocity, or alternating open/closed 16ths.', energyArc: 'meditative', rules: 5 rules from SPEC, ruleBreakers: 2 rule breakers from SPEC, blendsWith: ['techno', 'house', 'ukGarage'], vibeWords: ['dark', 'industrial', 'techno-edge', 'rolling', 'minimal', 'peak-hour', 'pressure'].

    The blendsWith values must use camelCase to match existing GENRES keys (ukGarage not uk_garage).
  </action>

  <verify>
    <automated>cd /Users/standard/Documents/New\ project\ 3 && node -e "const {GENRES} = require('./lib/genreDNA'); console.log('deepHouse' in GENRES, 'techHouse' in GENRES, GENRES.deepHouse.bpmRange[0], GENRES.techHouse.bpmRange[0])" 2>/dev/null || npx ts-node -e "import {GENRES} from './lib/genreDNA'; console.log('deepHouse' in GENRES, 'techHouse' in GENRES)" 2>/dev/null || echo "TypeScript compile check via tsc"</automated>
    Alternative: cd /Users/standard/Documents/New\ project\ 3 && npx tsc --noEmit 2>&1 | grep -i "genreDNA\|deepHouse\|techHouse" | head -10 — expect no errors on these lines
  </verify>

  <done>
    - lib/genreDNA.ts contains "deepHouse" key in the GENRES object (grep -c "deepHouse" lib/genreDNA.ts prints >= 1)
    - lib/genreDNA.ts contains "techHouse" key in the GENRES object (grep -c "techHouse" lib/genreDNA.ts prints >= 1)
    - npx tsc --noEmit exits 0 (no TypeScript errors)
    - deepHouse.bpmRange is [120, 124] and techHouse.bpmRange is [127, 132] in the file
  </done>
</task>

<task type="auto">
  <name>Task 2: CSS wiring + genre card UI in app/layout.tsx and app/page.tsx</name>

  <read_first>
    app/layout.tsx — read full file to confirm current import structure and path context for CSS import
    app/page.tsx — read full file to understand current 8-line stub before replacing
    web/styles.css — read the :root token block (lines 1–16) and .prompt-button class (lines 331–364) and .vst-settings details pattern (lines 637–665) — these are the CSS classes the new components extend
    app/globals.css — read if it exists to understand if the file already has content (relevant for deciding whether to add tokens there vs. importing web/styles.css directly)
    .planning/phases/01/01-UI-SPEC.md — full spec for component inventory, interaction contract, copywriting contract, and accessibility contract
    .planning/phases/01/01-SPEC.md — §5.1 for exact copy text (labels, sublines, descriptions, prompt templates)
  </read_first>

  <action>
    Step 1 — CSS wiring in app/layout.tsx:
    Add import '../web/styles.css' as the first line of app/layout.tsx. This supplies all var(--accent), var(--panel-strong), var(--line), var(--ink), var(--muted) tokens. If this import path fails at build time (path resolution error), fall back to copying the :root block from web/styles.css lines 1–16 into app/globals.css and adding import './globals.css' in layout.tsx instead.

    Step 2 — app/page.tsx genre cards UI:
    Replace the entire 8-line stub with a full 'use client' React component. The component needs useState for userInput and genreId. Structure:

    - Section heading "Pick your sound" in an h2 (no music vocabulary required)
    - House parent card as a details element with className "genre-parent-card". The summary shows "House" with a subline "Deep House · Tech House". Default collapsed (no open attribute). When expanded, shows two sub-genre buttons.
    - Deep House sub-genre card: button element with className "prompt-button genre-sub-card". Content: span "Deep House", small className "genre-subline" with "120–124 BPM · Soulful · Late-night", p className "genre-description" with "Slow, soulful, Rhodes-driven House. 120–124 BPM. Built for late-night dancefloors." onClick calls handleCardClick with prompt "Create an expanded Deep House song sketch with soulful chords, warm bassline, late-night groove, and jazzy lead phrases." and genreId "deepHouse".
    - Tech House sub-genre card: button element with same className pattern. Content: span "Tech House", small with "127–132 BPM · Dark · Peak-hour", p with "Dark, groove-locked House with a Techno edge. 127–132 BPM. Industrial textures and relentless kick pressure." onClick calls handleCardClick with prompt "Create an expanded Tech House song sketch with rolling bass, dark industrial textures, tight hi-hats, and relentless kick." and genreId "techHouse".
    - Direct-click cards for Techno, UK Garage, Trap: button elements with className "prompt-button" only (no genre-sub-card, no details wrapper per D-03). These fire their own prompt templates directly.

    Add a JSX style block or use className with scoped CSS for:
    - .genre-parent-card: background var(--panel-strong), border 1px solid var(--line), border-radius 8px, cursor pointer on summary
    - .genre-parent-card summary: list-style none; padding 16px
    - .genre-parent-card summary::marker and ::-webkit-details-marker: display none
    - .genre-parent-card summary h2::after: content "▸"; transition transform 200ms ease; color var(--muted)
    - .genre-parent-card[open] summary h2::after: transform rotate(90deg); color var(--accent)
    - .genre-sub-cards: display grid; gap 8px; padding 8px 16px 16px
    - @media (prefers-reduced-motion: no-preference): wrap transition declarations for the expand animation (UI-SPEC accessibility contract)
    - .genre-sub-card: no additional styles needed — reuses .prompt-button verbatim

    The existing prompt submission mechanism (textarea, submit button, result display) should be preserved from the original page.tsx if it exists. Since the stub is only 8 lines with no submission logic, add a simple form below the genre cards section: textarea bound to userInput, a submit button, and a pre element for displaying results. POST to /api/finisher with { genreId, userInput } on submit.

    All copy text must be verbatim from SPEC §5.1 — no rewriting permitted (UI-SPEC copywriting contract).
  </action>

  <verify>
    <automated>cd /Users/standard/Documents/New\ project\ 3 && npm run build 2>&1 | tail -20</automated>
    Expected: Build succeeds with no errors. If CSS import path fails, the error message will say "Module not found: ../web/styles.css" — in that case, copy the :root block to app/globals.css and adjust the import.
  </verify>

  <done>
    - npm run build exits 0
    - app/layout.tsx contains import statement referencing web/styles.css (or globals.css with token block copied)
    - app/page.tsx contains "genre-parent-card" className
    - app/page.tsx contains "deepHouse" and "techHouse" string literals (the genreId values)
    - app/page.tsx contains exact string "Create an expanded Deep House song sketch with soulful chords, warm bassline, late-night groove, and jazzy lead phrases." (verbatim from SPEC §5.1)
    - app/page.tsx contains exact string "Create an expanded Tech House song sketch with rolling bass, dark industrial textures, tight hi-hats, and relentless kick." (verbatim from SPEC §5.1)
    - app/page.tsx contains exact string "120–124 BPM · Soulful · Late-night" (verbatim from SPEC §5.1)
    - app/page.tsx contains exact string "127–132 BPM · Dark · Peak-hour" (verbatim from SPEC §5.1)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| genre card click → userInput state | Prompt templates are hardcoded constants — not user-controlled |
| userInput state → POST /api/finisher | Input crosses client→server boundary; existing route.ts validates genreId and userInput |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Tampering | genre card prompt injection | accept | prompt templates are hardcoded string constants from SPEC §5.1 — not user-editable DOM values; cannot be modified without code change |
| T-01-09 | Tampering | genreId value | mitigate | genreId is set from a hardcoded constant ("deepHouse"/"techHouse") not from user input; route.ts already validates GENRES[body.genreId] at line 436 — unknown keys return 400 |
| T-01-10 | Spoofing | /api/finisher endpoint | accept | Existing authentication model unchanged; route.ts already enforces input validation; no new attack surface added |
</threat_model>

<verification>
npm run build exits 0
grep -c "deepHouse" lib/genreDNA.ts returns >= 1
grep -c "genre-parent-card" app/page.tsx returns >= 1
grep -c "120–124 BPM · Soulful · Late-night" app/page.tsx returns 1
npx tsc --noEmit exits 0
</verification>

<success_criteria>
- lib/genreDNA.ts GENRES has "deepHouse" and "techHouse" camelCase keys
- app/layout.tsx imports web/styles.css (or equivalent token source)
- app/page.tsx renders House parent card (details/summary, collapsed default) per D-01 and D-02
- app/page.tsx renders Techno/UKG/Trap as direct-click prompt-button without expand per D-03
- Sub-genre card click sets genreId to "deepHouse" or "techHouse" (camelCase for route.ts validation)
- All SPEC §5.1 copy is verbatim in the component
- npm run build and npx tsc --noEmit exit 0
</success_criteria>

<output>
After completion, create .planning/phases/01-house-sub-genre-expansion/01-04-SUMMARY.md
</output>
