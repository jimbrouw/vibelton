---
phase: 01-house-sub-genre-expansion
plan: 05
type: execute
wave: 4
depends_on:
  - "01-03"
  - "01-04"
files_modified:
  - LISTENING_NOTES.md
autonomous: false
requirements:
  - SPEC §6 AC-7
must_haves:
  truths:
    - "LISTENING_NOTES.md exists with Deep House and Tech House listening sections"
    - "Jim has generated at least 1 Deep House sketch and marked it PASS or FAIL"
    - "Jim has generated at least 1 Tech House sketch and marked it PASS or FAIL"
    - "Both sketches marked PASS before phase is considered complete"
  artifacts:
    - path: "LISTENING_NOTES.md"
      provides: "Manual listening gate — AC-7 acceptance document"
      contains: "PASS"
  key_links:
    - from: "LISTENING_NOTES.md"
      to: "Phase 1 completion"
      via: "Manual sign-off gate per SPEC §6 AC-7"
      pattern: "PASS"
---

<objective>
Create LISTENING_NOTES.md template and run the manual listening gate for AC-7. Jim generates one Deep House sketch and one Tech House sketch, fills in the template, and marks both PASS before phase is considered complete.

Purpose: AC-7 is the final acceptance gate. No automated test can verify that the music sounds soulful/late-night (Deep House) or dark/peak-hour (Tech House) — only Jim can.
Output: LISTENING_NOTES.md filled with Jim's assessment of at least one sketch per sub-genre.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01/01-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LISTENING_NOTES.md acceptance gate template</name>

  <read_first>
    LISTENING_NOTES.md — read the existing file if it exists (it may already exist from Phase 0 — check and extend rather than overwrite if it contains Phase 0 notes)
    .planning/phases/01/01-SPEC.md — §6 AC-7 pass conditions and the full acceptance criteria table
  </read_first>

  <action>
    Check if LISTENING_NOTES.md already exists. If it has content from Phase 0 (manual listening notes for prior work), append the Phase 1 section below existing content. If the file is empty or does not exist, create it.

    Add a Phase 1 section with:
    - Phase header: "## Phase 1 — House Sub-Genre Expansion"
    - Instructions: "Generate each sketch using the genre card UI at http://localhost:3000 or via the Deep/Tech House prompt templates. Listen to the resulting Ableton sketch. Mark each as PASS or FAIL."
    - AC criteria checklist reminder sourced from SPEC §6 (BPM, kick divergence, bass extension, card renders, vocabulary-free cards)
    - Deep House listening section with fields: Date, Prompt used, BPM observed, Intro style observed (drums_reveal/atmosphere/filtered), Character assessment (soulful/late-night feel), Verdict (PASS/FAIL), Notes
    - Tech House listening section with fields: Date, Prompt used, BPM observed, Intro style observed, Character assessment (dark/peak-hour feel), Verdict (PASS/FAIL), Notes
    - Phase 1 overall verdict line: "Phase 1 AC-7 Status: [ PASS / FAIL ]"
    - Sign-off line: "Signed off by: [Jim] on [date]"

    Use markdown table or structured header/value format — whichever matches any existing Phase 0 format in the file. Do not delete Phase 0 content if present.
  </action>

  <verify>
    <automated>grep -c "Phase 1" /Users/standard/Documents/New\ project\ 3/LISTENING_NOTES.md</automated>
    Expected: >= 1 (the Phase 1 header exists in the file)
  </verify>

  <done>
    - LISTENING_NOTES.md contains "Phase 1" section header
    - LISTENING_NOTES.md contains "Deep House" and "Tech House" subsections with Verdict fields
    - LISTENING_NOTES.md contains "AC-7 Status" line
    - File is not empty and does not delete prior Phase 0 content if it existed
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual listening gate — AC-7</name>

  <what-built>
    LISTENING_NOTES.md template created. Python backend (genre_dna.py, harmony.py, planner.py) and Next.js UI (app/page.tsx) from Plans 02–04 are complete. All automated tests pass. The system can generate Deep House and Tech House sketches.
  </what-built>

  <how-to-verify>
    1. Start the Next.js dev server: npm run dev in the project root
    2. Open http://localhost:3000 in a browser
    3. Verify the genre cards section renders with a "Pick your sound" heading
    4. Click the "House" parent card — it should expand to reveal Deep House and Tech House sub-genre cards
    5. Click "Deep House" — verify the prompt textarea populates with "Create an expanded Deep House song sketch with soulful chords, warm bassline, late-night groove, and jazzy lead phrases."
    6. Submit the prompt (via the Generate button or equivalent). Open the resulting .als or MIDI output in Ableton
    7. Listen: Does the BPM land in 120–124? Does the intro feel patient and atmospheric (drums-only, or chords-first, or full-but-filtered)? Does it have a soulful, late-night character distinct from generic House?
    8. Fill in the "Deep House" section of LISTENING_NOTES.md with your assessment. Mark PASS or FAIL.
    9. Click "Tech House" on the UI. Submit the prompt. Open in Ableton.
    10. Listen: Does the BPM land in 127–132? Does the kick feel relentless and machine-like? Is the intro tight and building tension (Techno-influenced feel)? Is it sonically distinct from Deep House and generic House?
    11. Fill in the "Tech House" section of LISTENING_NOTES.md. Mark PASS or FAIL.
    12. Update "Phase 1 AC-7 Status" to PASS if both pass, FAIL if either fails.
  </how-to-verify>

  <resume-signal>
    If both Deep House and Tech House are marked PASS in LISTENING_NOTES.md: type "approved" to complete Phase 1.
    If either is marked FAIL: describe what was wrong (e.g., "Deep House BPM was 128 not 120-124" or "kick patterns sound identical"). Claude will diagnose and fix before re-listening.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LISTENING_NOTES.md | Static markdown file; human writes to it; no code execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11 | Repudiation | LISTENING_NOTES.md | accept | Document records date and Jim's sign-off; sufficient for a solo developer workflow |
</threat_model>

<verification>
LISTENING_NOTES.md exists with Phase 1 section and both sub-genre Verdict fields
Jim marks both PASS in the document
</verification>

<success_criteria>
- LISTENING_NOTES.md Phase 1 section exists with Deep House and Tech House entries
- Jim has filled Verdict fields with PASS for both sub-genres
- "Phase 1 AC-7 Status: PASS" is written in the document
- Jim has typed "approved" to signal phase completion
</success_criteria>

<output>
After completion, create .planning/phases/01-house-sub-genre-expansion/01-05-SUMMARY.md
</output>
