---
phase: 01-house-sub-genre-expansion
plan: 02
type: tdd
wave: 2
depends_on:
  - "01-01"
files_modified:
  - vibelton/genre_dna.py
autonomous: true
requirements:
  - SPEC §3.1
  - SPEC §3.2
  - SPEC §3.3
  - SPEC §3.4
  - SPEC §4
must_haves:
  truths:
    - "GenreDNA dataclass has a parent field (str | None = None)"
    - "GROOVE_TEMPLATES contains 'laid_back' and 'push' entries"
    - "DEEP_HOUSE and TECH_HOUSE GenreDNA constants exist with correct drum/bass/lead patterns per SPEC §3.3–3.4"
    - "_resolve_genre() merges parent bass_patterns + lead_phrases into child at module load"
    - "RESOLVED_GENRE_REGISTRY is a public module-level name"
    - "RESOLVED_GENRE_REGISTRY['deep_house'].bass_patterns has length >= 5 (AC-4)"
    - "D-05: CURATED_GENRES['deep_house'].structure == ('intro 32','build 16','main 32','break 16','main 2 32','outro 32') (160 bars total per D-05)"
    - "D-06: CURATED_GENRES['tech_house'].structure == ('intro 16','build 32','peak 32','reduction 16','peak 2 32','outro 16') (144 bars total per D-06)"
    - "CURATED_GENRES contains 'deep_house' and 'tech_house' entries with correct BPM ranges"
    - "CURATED_ALIASES maps 'deep house' → 'deep_house' and 'tech house' → 'tech_house'"
    - "detect_style_from_text('Create a deep house track') returns 'deep_house'"
    - "detect_style_from_text('Create a tech house track') returns 'tech_house'"
    - "lookup_genre() queries _RESOLVED_GENRE_REGISTRY"
  artifacts:
    - path: "vibelton/genre_dna.py"
      provides: "Parent-pointer inheritance + DEEP_HOUSE + TECH_HOUSE + resolved registry + detect_style extension"
      exports:
        - "RESOLVED_GENRE_REGISTRY"
        - "DEEP_HOUSE"
        - "TECH_HOUSE"
        - "_resolve_genre"
        - "_RESOLVED_GENRE_REGISTRY"
  key_links:
    - from: "DEEP_HOUSE (GenreDNA)"
      to: "HOUSE (GenreDNA)"
      via: "parent='house' field + _resolve_genre() at module load"
      pattern: "parent.*house"
    - from: "_RESOLVED_GENRE_REGISTRY"
      to: "GENRE_REGISTRY"
      via: "_resolve_genre() dict comprehension"
      pattern: "_resolve_genre"
    - from: "lookup_genre()"
      to: "_RESOLVED_GENRE_REGISTRY"
      via: "_RESOLVED_GENRE_REGISTRY.get()"
      pattern: "_RESOLVED_GENRE_REGISTRY\\.get"
---

<objective>
Implement the full Python data model for Phase 1: parent-pointer inheritance mechanism, GROOVE_TEMPLATES additions, DEEP_HOUSE and TECH_HOUSE GenreDNA constants, CuratedGenreDNA entries, and detect_style_from_text() extension.

Purpose: Turn Plan 01's RED tests GREEN. This is the core musical intelligence work for Phase 1.
Output: vibelton/genre_dna.py with inheritance infrastructure, two new sub-genre constants, and extended detection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01/01-SPEC.md
@.planning/phases/01/01-CONTEXT.md
@.planning/phases/01/01-PATTERNS.md

<interfaces>
From .planning/phases/01/01-PATTERNS.md — key existing patterns in vibelton/genre_dna.py:

GenreDNA dataclass (lines 111–138): frozen dataclass with fields: name, bpm_default, bpm_range, groove_profile, kick_patterns, snare_patterns, hat_patterns, hat_style, bass_octave, bass_patterns, chord_octave, chord_style, chord_rhythms, chord_duration, chord_extensions, lead_octave, lead_phrases. Add parent: str | None = None as the LAST field (has a default, so all existing callsites unaffected).

GROOVE_TEMPLATES (lines 58–108): dict[str, GrooveProfile]. GrooveProfile.from_swing(swing_float, name_str) factory exists. Use from_swing(0.62, "laid_back") for laid_back. Build "push" as a hand-crafted GrooveProfile with position_jitter_ms=3.0, velocity_jitter=4, length_jitter=0.007, weak_beat_curve=(1.0, 0.88, 0.96, 0.84), push_pull_per_subdivision=(-0.005, 0.0, -0.003, 0.0).

GENRE_REGISTRY (lines 708–733): flat dict[str, GenreDNA] mapping lowercase names to constants. Add "deep_house": DEEP_HOUSE and "tech_house": TECH_HOUSE entries.

lookup_genre() (line 1003–1005): currently returns GENRE_REGISTRY.get(name.lower().strip(), DEFAULT_DNA). Must change to _RESOLVED_GENRE_REGISTRY.get().

detect_style_from_text() (lines 1154–1202): if/elif priority chain. "techno" check is at approximately line 1175. Sub-genre checks must be inserted BEFORE the "techno" check.

CURATED_GENRES (lines 738–813): dict[str, CuratedGenreDNA]. CuratedGenreDNA fields: name, bpm_range, swing_percent, bar_grid, structure (tuple of str), kick_pattern, bass_pattern, hi_hat_pattern, energy_arc, rules (tuple), rule_breakers (tuple), blends_with (tuple), vibe_words (tuple).

CURATED_ALIASES (approximately line 999): dict[str, str] mapping alternate names to canonical keys.

HOUSE GenreDNA bass_patterns has 3 patterns (per SPEC §3.2, AC-4 requires >= 5 after merge: 3 parent + 2 child).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add parent field + GROOVE_TEMPLATES entries + _resolve_genre function</name>

  <read_first>
    vibelton/genre_dna.py — read FULL file before touching anything; this is a large file — understand the existing structure of GenreDNA dataclass, GROOVE_TEMPLATES dict, GENRE_REGISTRY dict, and lookup_genre() function
    .planning/phases/01/01-SPEC.md — §3.1 (parent field), §3.2 (merge rules), SPEC §3.3 groove_profile="laid_back", SPEC §3.4 groove_profile="push"
    .planning/phases/01/01-PATTERNS.md — Sub-task B (GROOVE_TEMPLATES) and Sub-task C (_resolve_genre pattern) code excerpts
  </read_first>

  <behavior>
    - After edit: GenreDNA dataclass has field parent: str | None = None as last field
    - After edit: GROOVE_TEMPLATES["laid_back"] exists and is a GrooveProfile built with from_swing(0.62, "laid_back")
    - After edit: GROOVE_TEMPLATES["push"] exists and is a GrooveProfile with position_jitter_ms=3.0
    - After edit: _resolve_genre(key, registry) function exists, handles parent=None (returns child), handles missing parent (returns child), raises ValueError on circular parent
    - After edit: lookup_genre() queries _RESOLVED_GENRE_REGISTRY not GENRE_REGISTRY
    - After edit: import dataclasses is added at module top (needed for dataclasses.replace in _resolve_genre)
    - _RESOLVED_GENRE_REGISTRY comprehension is NOT placed in this task — it is placed by Task 2 after all GENRE_REGISTRY mutations are complete
  </behavior>

  <action>
    1. Add import dataclasses at the top of the file alongside the existing from dataclasses import dataclass (if not already present as a module import).
    2. In the GenreDNA frozen dataclass, append parent: str | None = None as the last field — after lead_phrases. The field must have a default value so all 10+ existing genre constant definitions (HOUSE, TECHNO, etc.) remain valid without modification.
    3. After the GROOVE_TEMPLATES dict definition closes, add two entries: GROOVE_TEMPLATES["laid_back"] = GrooveProfile.from_swing(0.62, "laid_back") and GROOVE_TEMPLATES["push"] = GrooveProfile(name="push", position_jitter_ms=3.0, velocity_jitter=4, length_jitter=0.007, weak_beat_curve=(1.0, 0.88, 0.96, 0.84), push_pull_per_subdivision=(-0.005, 0.0, -0.003, 0.0)).
    4. After the GENRE_REGISTRY dict definition (not inside it), add the _resolve_genre() function per the PATTERNS.md excerpt: takes key and registry params, returns child unchanged if parent is None or parent key not in registry, raises ValueError if child.parent == key (circular), otherwise returns dataclasses.replace(child, bass_patterns=parent.bass_patterns + child.bass_patterns, lead_phrases=parent.lead_phrases + child.lead_phrases).
    5. Update lookup_genre() to call _RESOLVED_GENRE_REGISTRY.get(name.lower().strip(), DEFAULT_DNA) instead of GENRE_REGISTRY.get(). Note: _RESOLVED_GENRE_REGISTRY will be defined by Task 2 — this forward reference is intentional; lookup_genre() is called at runtime, not at module load, so definition order is fine.
    DO NOT place the _RESOLVED_GENRE_REGISTRY dict comprehension or RESOLVED_GENRE_REGISTRY public alias in this task. Task 2 will place both in the correct final position after all GENRE_REGISTRY mutations.
  </action>

  <verify>
    <automated>cd /Users/standard/Documents/New\ project\ 3 && python -c "from vibelton.genre_dna import GROOVE_TEMPLATES; print('laid_back' in GROOVE_TEMPLATES, 'push' in GROOVE_TEMPLATES)"</automated>
    Expected output: True True
  </verify>

  <done>
    - python -c "from vibelton.genre_dna import GROOVE_TEMPLATES; print('laid_back' in GROOVE_TEMPLATES, 'push' in GROOVE_TEMPLATES)" prints True True
    - python -c "from vibelton.genre_dna import GenreDNA; import inspect; print('parent' in [f.name for f in GenreDNA.__dataclass_fields__.values()])" prints True
    - _resolve_genre() function exists in vibelton/genre_dna.py (grep confirms)
    - All existing tests still pass: python -m pytest tests/test_genre_dna.py -k "groove_profiles or groove_templates" -x -q
    - _RESOLVED_GENRE_REGISTRY comprehension is NOT present yet (that is Task 2's responsibility)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add DEEP_HOUSE + TECH_HOUSE constants, CURATED_GENRES entries, detect_style extension, and _RESOLVED_GENRE_REGISTRY (last)</name>

  <read_first>
    vibelton/genre_dna.py — read the HOUSE GenreDNA constant (to copy its bass_patterns and lead_phrases as the parent patterns that will be prepended), the GENRE_REGISTRY dict (to know where to insert sub-genre keys), the CURATED_GENRES dict (to follow CuratedGenreDNA field order), the CURATED_ALIASES dict (to add "deep house"/"tech house" aliases), and detect_style_from_text() (to find the exact line where "techno" check begins)
    .planning/phases/01/01-SPEC.md — §3.3 (DEEP_HOUSE field values table), §3.4 (TECH_HOUSE field values table), §4 (keyword lists), D-05 and D-06 section structures
    .planning/phases/01/01-PATTERNS.md — Sub-tasks C and D code excerpts for GenreDNA constants and CURATED_GENRES entries
    .planning/phases/01/01-CONTEXT.md — D-05 (Deep House structure), D-06 (Tech House structure), locked decisions
  </read_first>

  <behavior>
    - DEEP_HOUSE GenreDNA: parent="house", bpm_range=(120,124), swing=0.62 (per SPEC §3.3), groove_profile=GROOVE_TEMPLATES["laid_back"], kick_patterns has 1 pattern (four-on-floor + beat-3 ghost at vel 55), snare_patterns has 1 pattern (beats 2 and 4, vel 90-100), hat_patterns has 2 patterns (8th-note closed; open hat on upbeats), bass_patterns has 2 child patterns (sub-bass focused, octave drops on bar 3), lead_phrases has 2 child phrases (slow stepwise)
    - TECH_HOUSE GenreDNA: parent="house", bpm_range=(127,132), swing=0.50, groove_profile=GROOVE_TEMPLATES["push"], kick_patterns has 1 pattern (four-on-floor, vel 110-127 all beats, no ghost), snare_patterns has 1 pattern (clap on 2+4 + sidestick ghost on "and" of 3), hat_patterns has 2 patterns (16th-note closed high vel; alternating open/closed 16ths), bass_patterns has 2 child patterns (tight 16th-note riff), lead_phrases has 2 child phrases (short staccato minor 3rd motif)
    - GENRE_REGISTRY contains "deep_house": DEEP_HOUSE and "tech_house": TECH_HOUSE
    - _RESOLVED_GENRE_REGISTRY["deep_house"].bass_patterns length == len(HOUSE.bass_patterns) + 2 (>= 5)
    - CURATED_GENRES["deep_house"] has bpm_range=(120,124), structure=("intro 32","build 16","main 32","break 16","main 2 32","outro 32") per D-05
    - CURATED_GENRES["tech_house"] has bpm_range=(127,132), structure=("intro 16","build 32","peak 32","reduction 16","peak 2 32","outro 16") per D-06
    - CURATED_GENRES["deep_house"].vibe_words == ("deep","soulful","late-night","rhodes","warm","hypnotic","jazzy")
    - CURATED_GENRES["tech_house"].vibe_words == ("dark","industrial","techno-edge","rolling","minimal","peak-hour","pressure")
    - CURATED_ALIASES maps "deep house" → "deep_house" and "tech house" → "tech_house"
    - detect_style_from_text("Create a deep house track") == "deep_house"
    - detect_style_from_text("Create a tech house track") == "tech_house"
    - detect_style_from_text("Create a techno track") still == "techno" (regression guard)
    - detect_style_from_text("Create a house track") still == "house" (regression guard)
    - _RESOLVED_GENRE_REGISTRY comprehension is placed as the LAST definition after all GENRE_REGISTRY mutations
  </behavior>

  <action>
    1. Add DEEP_HOUSE GenreDNA constant before the GENRE_REGISTRY dict. Fields per SPEC §3.3: parent="house", bpm_default=122, bpm_range=(120,124), groove_profile=GROOVE_TEMPLATES["laid_back"], chord_style="long_tone", chord_duration=4.0 (whole bar), chord_extensions=(). Kick pattern: single pattern [0.0,1.0,2.0,3.0] with note at beat 2.0 at velocity 55 as ghost — represent as ({"beat": 0.0, "vel": 110}, {"beat": 1.0, "vel": 110}, {"beat": 2.0, "vel": 55}, {"beat": 3.0, "vel": 110}) or follow the exact tuple-of-list format used by HOUSE.kick_patterns. Bass patterns: 2 child patterns — first a sub-bass root pattern with octave drop on bar 3 beat, second an octave-movement pattern. Lead phrases: 2 child phrases — slow stepwise motion, 2-3 notes per 4 bars.
    2. Add TECH_HOUSE GenreDNA constant after DEEP_HOUSE. Fields per SPEC §3.4: parent="house", bpm_default=129, bpm_range=(127,132), groove_profile=GROOVE_TEMPLATES["push"], chord_style="stab", chord_duration=0.5 (8th-note), chord_extensions=(). Kick pattern: single pattern with all 4 beats at velocity 120 (hard, no ghost). Bass patterns: 2 child patterns — tight 16th-note riff (0.0, 0.25, 0.5, 0.75 per beat), industrial rolling pattern. Lead phrases: 2 child phrases — short staccato, minor 3rd interval motif.
    3. Add "deep_house": DEEP_HOUSE and "tech_house": TECH_HOUSE to GENRE_REGISTRY dict. Place after existing entries.
    4. Place _RESOLVED_GENRE_REGISTRY as the LAST definition after all GENRE_REGISTRY mutations. The correct final definition order in the file must be: (1) GENRE_REGISTRY dict literal with all top-level genres, (2) "deep_house": DEEP_HOUSE and "tech_house": TECH_HOUSE added to GENRE_REGISTRY (either inline in the dict literal or as assignment statements), (3) _resolve_genre() function (already placed by Task 1), (4) _RESOLVED_GENRE_REGISTRY dict comprehension: _RESOLVED_GENRE_REGISTRY: dict[str, GenreDNA] = {key: _resolve_genre(key, GENRE_REGISTRY) for key in GENRE_REGISTRY}, (5) RESOLVED_GENRE_REGISTRY = _RESOLVED_GENRE_REGISTRY public alias. Do NOT place _RESOLVED_GENRE_REGISTRY before the GENRE_REGISTRY["deep_house"] and GENRE_REGISTRY["tech_house"] additions — the comprehension runs at module load time and must execute after all mutations so that deep_house and tech_house are included in the resolved registry.
    5. Add CURATED_GENRES["deep_house"] and CURATED_GENRES["tech_house"] entries after existing CURATED_GENRES entries. Use exact field values from SPEC §3.3–3.4 and D-05/D-06 section structures. rule_breakers and vibe_words exact values per SPEC.
    6. Add to CURATED_ALIASES: "deep house": "deep_house" and "tech house": "tech_house".
    7. In detect_style_from_text(), insert two new if-blocks BEFORE the "techno" check (currently at approximately line 1175). First block: any(w in lowered for w in ["deep house","soulful house","late-night house","rhodes house"]) → return "deep_house". Second block: any(w in lowered for w in ["tech house","techno house","dark house","industrial house","peak-hour house"]) → return "tech_house". Then the existing "techno" check follows unchanged.
  </action>

  <verify>
    <automated>cd "/Users/standard/Documents/New project 3" && python -m pytest tests/test_genre_dna.py -x -q 2>&1 | tail -20 && python -c "from vibelton.genre_dna import RESOLVED_GENRE_REGISTRY; assert 'deep_house' in RESOLVED_GENRE_REGISTRY and 'tech_house' in RESOLVED_GENRE_REGISTRY, 'sub-genres missing from RESOLVED_GENRE_REGISTRY'; print('RESOLVED_GENRE_REGISTRY OK')"</automated>
    Expected: All tests pass (GREEN) and RESOLVED_GENRE_REGISTRY import assertion passes.
  </verify>

  <done>
    - python -m pytest tests/test_genre_dna.py -x -q exits 0 (all tests green)
    - python -c "from vibelton.genre_dna import RESOLVED_GENRE_REGISTRY; print(len(RESOLVED_GENRE_REGISTRY['deep_house'].bass_patterns))" prints >= 5
    - python -c "from vibelton.genre_dna import CURATED_GENRES; print(CURATED_GENRES['deep_house'].bpm_range, CURATED_GENRES['tech_house'].bpm_range)" prints (120, 124) (127, 132)
    - python -c "from vibelton.genre_dna import detect_style_from_text; print(detect_style_from_text('Create a deep house track'), detect_style_from_text('Create a tech house track'))" prints deep_house tech_house
    - _RESOLVED_GENRE_REGISTRY comprehension appears in vibelton/genre_dna.py AFTER the GENRE_REGISTRY["deep_house"] and GENRE_REGISTRY["tech_house"] entries (verify line ordering with grep -n)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| module import → genre_dna | Module-level constants; no external input at this layer |
| detect_style_from_text() | Receives user text string from planner; low-trust input lowercased internally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Tampering | GENRE_REGISTRY mutation | mitigate | DEEP_HOUSE and TECH_HOUSE must be frozen dataclasses (frozen=True already on GenreDNA) — values cannot be mutated at runtime |
| T-01-03 | Denial of Service | _resolve_genre circular parent | mitigate | Raise ValueError at module load time if child.parent == key (circular detection per SPEC §3.2) — fails fast at import, not during request |
| T-01-04 | Information Disclosure | detect_style_from_text | accept | Input is user music prompt, not credentials; lowercasing and substring match is safe |
</threat_model>

<verification>
python -m pytest tests/test_genre_dna.py -x -q — all 15+ tests green
python -m pytest tests/ -x -q — full suite green
</verification>

<success_criteria>
- All Plan 01 RED tests now GREEN
- RESOLVED_GENRE_REGISTRY["deep_house"].bass_patterns length >= 5 (AC-4)
- CURATED_GENRES["deep_house"].bpm_range == (120, 124) (AC-1)
- CURATED_GENRES["tech_house"].bpm_range == (127, 132) (AC-2)
- Deep House kick_pattern string != Tech House kick_pattern string (AC-3)
- detect_style_from_text returns "deep_house" and "tech_house" for sub-genre prompts
- detect_style_from_text("techno") and ("house") still return "techno" and "house" (regressions clean)
- _RESOLVED_GENRE_REGISTRY comprehension is the LAST registry definition after all GENRE_REGISTRY mutations
- python -m pytest tests/ -x -q exits 0
</success_criteria>

<output>
After completion, create .planning/phases/01-house-sub-genre-expansion/01-02-SUMMARY.md
</output>
