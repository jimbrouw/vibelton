---
phase: 01-house-sub-genre-expansion
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_genre_dna.py
autonomous: true
requirements:
  - SPEC §3.1
  - SPEC §3.2
  - SPEC §3.3
  - SPEC §3.4
  - SPEC §4
must_haves:
  truths:
    - "Failing tests exist for AC-1 (Deep House BPM), AC-2 (Tech House BPM), AC-3 (kick divergence), AC-4 (bass list extension >= 5)"
    - "Existing test for groove templates is updated to require 'laid_back' and 'push'"
    - "Existing test for CURATED_GENRES is updated to require 'deep_house' and 'tech_house' keys"
    - "All new tests fail (RED) before Plan 02 runs"
  artifacts:
    - path: "tests/test_genre_dna.py"
      provides: "RED test stubs for Phase 1 acceptance criteria AC-1 through AC-4"
      contains: "RESOLVED_GENRE_REGISTRY"
  key_links:
    - from: "tests/test_genre_dna.py"
      to: "vibelton/genre_dna.RESOLVED_GENRE_REGISTRY"
      via: "from vibelton.genre_dna import RESOLVED_GENRE_REGISTRY"
      pattern: "RESOLVED_GENRE_REGISTRY"
---

<objective>
Write the RED phase of TDD for Phase 1 acceptance criteria. Add failing test stubs to tests/test_genre_dna.py before any production code changes.

Purpose: Establish the contract that Plan 02 must satisfy. These tests fail now, pass after Plan 02 ships.
Output: Updated tests/test_genre_dna.py with 6 new/updated test methods covering AC-1 through AC-4 plus groove templates and CURATED_GENRES key set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01/01-SPEC.md
@.planning/phases/01/01-VALIDATION.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Add Wave 0 test stubs — RED phase</name>

  <read_first>
    tests/test_genre_dna.py — read full file to understand existing import block, class structure, and assertion patterns before touching anything
    .planning/phases/01/01-SPEC.md — AC-1 through AC-4 exact pass conditions
    .planning/phases/01/01-PATTERNS.md — tests/test_genre_dna.py Pattern section for exact code analogs
  </read_first>

  <behavior>
    - test_required_groove_templates_exist: currently asserts subset {"mpc_58", "mpc_62", "dnb_tight", "ukg_shuffle", "dembow", "trap_triplet"} — must be updated to also require "laid_back" and "push" in that same set
    - test_curated_genre_prompt_layer_covers_phase_one_genres: currently asserts set equals 10 keys — must be updated to assert 12 keys including "deep_house" and "tech_house"
    - test_deep_house_bpm_range (new): CURATED_GENRES["deep_house"].bpm_range == (120, 124) — AC-1
    - test_tech_house_bpm_range (new): CURATED_GENRES["tech_house"].bpm_range == (127, 132) — AC-2
    - test_kick_pattern_divergence (new): CURATED_GENRES["deep_house"].kick_pattern != CURATED_GENRES["tech_house"].kick_pattern — AC-3
    - test_bass_list_extended_by_parent (new): len(RESOLVED_GENRE_REGISTRY["deep_house"].bass_patterns) >= 5 — AC-4
    - test_detect_style_deep_house (new): detect_style_from_text("Create a deep house track") == "deep_house"
    - test_detect_style_tech_house (new): detect_style_from_text("Create a tech house track") == "tech_house"
  </behavior>

  <action>
    1. Add RESOLVED_GENRE_REGISTRY to the imports block — the existing import is from vibelton.genre_dna and already imports several symbols; add RESOLVED_GENRE_REGISTRY to that same from-import list.
    2. In test_required_groove_templates_exist, extend the set literal to include "laid_back" and "push" alongside the existing 6 entries (the assertTrue issubset call means adding to the set, not replacing it).
    3. In test_curated_genre_prompt_layer_covers_phase_one_genres, add "deep_house" and "tech_house" to the assertEqual set literal (changes from 10-key set to 12-key set). The rest of the method body (build_curated_prompt_context call and asserts) stays unchanged.
    4. Add 6 new test methods to GenreDNATest after the existing methods: test_deep_house_bpm_range, test_tech_house_bpm_range, test_kick_pattern_divergence, test_bass_list_extended_by_parent, test_detect_style_deep_house, test_detect_style_tech_house. Each uses self.assertEqual or self.assertGreaterEqual or self.assertNotEqual — matching existing assertion style, no subTest needed since these are single assertions.
    5. Do NOT import anything from planner.py — tests stay in the test_genre_dna.py surface only.
  </action>

  <verify>
    <automated>cd /Users/standard/Documents/New\ project\ 3 && python -m pytest tests/test_genre_dna.py -x -q 2>&1 | tail -20</automated>
    Expected: Multiple failures — ImportError or AttributeError on RESOLVED_GENRE_REGISTRY, and KeyError on CURATED_GENRES["deep_house"]. Some existing tests may still pass. RED state confirmed when new test methods are present and failing.
  </verify>

  <done>
    - tests/test_genre_dna.py imports RESOLVED_GENRE_REGISTRY from vibelton.genre_dna
    - test_required_groove_templates_exist set literal contains "laid_back" and "push"
    - test_curated_genre_prompt_layer_covers_phase_one_genres expected set contains "deep_house" and "tech_house"
    - 6 new test methods exist in GenreDNATest: test_deep_house_bpm_range, test_tech_house_bpm_range, test_kick_pattern_divergence, test_bass_list_extended_by_parent, test_detect_style_deep_house, test_detect_style_tech_house
    - Running python -m pytest tests/test_genre_dna.py shows failures (RED) — not all green
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file → production module | Tests import from vibelton.genre_dna; no external input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | tests/test_genre_dna.py | accept | Test file only; no security surface; no user input |
</threat_model>

<verification>
python -m pytest tests/test_genre_dna.py -x -q shows new test methods present and failing (RED state confirmed)
</verification>

<success_criteria>
- 6 new/updated test methods in tests/test_genre_dna.py
- RESOLVED_GENRE_REGISTRY in imports
- All 6 new tests fail with errors (ImportError, KeyError, or AssertionError) — RED confirmed
- No production code touched in this plan
</success_criteria>

<output>
After completion, create .planning/phases/01-house-sub-genre-expansion/01-01-SUMMARY.md
</output>
