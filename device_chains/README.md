# Device Chain Placeholders

[Unverified] These `.adv` files have not been created or tested in Ableton Live.

Stage 1.5 uses `device_chains.json` as the sound-chain target list. Until bridge loading is tested, load matching Ableton-native devices manually based on each entry's `manual_load` note.

Required placeholder families:

- `house_techno`: kick, bass, pad, lead, keys
- `hiphop_lofi`: kick, bass, pad, lead, keys
- `dnb_ukg`: kick, bass, pad, lead, keys
